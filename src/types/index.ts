/**
 * Shared TypeScript type definitions for CollegeZen.
 * Add application-wide types here as features are built.
 */

export interface NavLink {
  label: string;
  href: string;
}

export interface SiteConfig {
  name: string;
  description: string;
  url: string;
}
